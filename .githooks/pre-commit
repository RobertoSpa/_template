#!/usr/bin/env bash
set -euo pipefail

if ! command -v mise >/dev/null; then
  echo 'pre-commit: mise is missing. Do the mise step of the setup list in README.md.' >&2
  exit 1
fi
mise exec -- gitleaks git --staged --no-banner --redact .
pnpm lint
pnpm lint:fsd
pnpm check
