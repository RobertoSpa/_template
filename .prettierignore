pnpm-lock.yaml
graphify-out

# Markdown follows the simple-english and technical-writing rules, and
# prettier rewrites the sample payloads inside its fenced code blocks.
*.md

.impeccable
reports/
.stryker-tmp/
# The local permissions of Claude Code. Git ignores it, and CI never sees it.
.claude/settings.local.json
